# 2026-05-13 日志

## 完成的工作

### ChatPage 主题重构
- 移除 `ChatThemeExtension` 类（约90行代码）
- 将所有 `ChatThemeExtension.of(context).*` 调用替换为直接使用 `Theme.of(context).colorScheme.*`
- 文件结构修复：解决了因删除类导致的括号不匹配问题
- `flutter analyze` 通过，仅有4个 `prefer_const_constructors` 提示

### 关键修复点
- `peerBubbleColor` 需要判断深色模式：`isDark ? Color(0xFF3A3A3C) : colorScheme.surface`
- `iconBarBgColor` 和 `emojiPickerBgColor` 深浅模式颜色不同
- 所有颜色计算逻辑内联到各个方法中，而非通过 ThemeExtension 统一获取

## 文件变更
- `lib/pages/message/chat_page.dart`：移除 ChatThemeExtension，直接使用 colorScheme

### 第二阶段重构：移除 isDark 判断
- 继续移除所有 `isDark` 三元判断，将颜色直接映射到 `colorScheme.*` 属性
- 主要替换映射：
  - `isDark ? Colors.white12 : Colors.grey[300]` → `colorScheme.outline.withValues(alpha: 0.3)`
  - `isDark ? Color(0xFF3A3A3C) : colorScheme.surface` → `colorScheme.surfaceContainer`
  - `isDark ? Color(0xFF2C2C2C) : Color(0xFFF5F5F5)` → `colorScheme.surfaceContainer`
  - `isDark ? Color(0xFF1C1C1E) : Colors.white` → `colorScheme.surface`
  - `isDark ? Colors.white : Colors.black87` → `colorScheme.onSurface`
  - `isDark ? Colors.white : colorScheme.onPrimary` → `colorScheme.onPrimary`
- 修复了替换过程中引入的双逗号语法错误
- `flutter analyze` 通过，仅有4个 `prefer_const_constructors` 提示
- 所有颜色现在完全依赖 `colorScheme` 的内置语义化属性，自动适配深色/浅色模式

---

## 新增 WebRTC 视频通话模块

### 后端（cuberverse 项目已有）
- NestJS 信令服务器，端口 3002
- 协议：WebSocket JSON（event + data）
- 事件：connected, join-room, leave-room, user-joined, user-left, room-users, offer, answer, ice-candidate, end-call, call-ended

### Flutter 端新增文件
| 文件 | 说明 |
|------|------|
| `lib/webrtc/models/call_state.dart` | 通话状态枚举（idle/calling/incoming/connecting/connected/ended/error） |
| `lib/webrtc/models/signaling_message.dart` | 信令消息模型（base + 各事件类型） |
| `lib/webrtc/services/signaling_client.dart` | WebSocket 信令客户端（GetxService，全局单例） |
| `lib/webrtc/services/webrtc_controller.dart` | WebRTC 核心控制器（GetxController，管理 PC/媒体流/信令协调/通话状态） |
| `lib/webrtc/pages/call_entry_page.dart` | 入口页：输入服务器地址/房间号/昵称 |
| `lib/webrtc/pages/video_call_page.dart` | 视频通话页：远端主画面 + 本地画中画 + 底部控制栏（麦克风/摄像头/翻转/挂断）|

### 修改的文件
- `pubspec.yaml`：添加 `flutter_webrtc: ^0.12.0` 依赖
- `lib/main.dart`：注册 `SignalingClient` 全局单例
- `lib/routes/app_routes.dart`：添加 `/webrtc-call` 路由

### 架构设计
- 每个通话生命周期创建新的 `WebrtcController`（`Get.put(permanent:true)` 后 `Get.delete` 清理）
- `SignalingClient` 全局单例，WebSocket 连接由控制器的 `startCall()` 触发，`endCall()` 时断开
- STUN 服务器使用 Google 公共 STUN（stun:stun.l.google.com:19302）
- UI 使用 `Obx` 响应式更新通话状态，PopScope 防止误触返回

### WebRTC SIGABRT 崩溃修复（2026-05-13 下午）
- **错误**：连接服务器等待中闪退，`SIGABRT` / `Check failed: !IsUnifiedPlan()`
- **根因**：`_pc!.addStream(_localStream!)` 使用已废弃的 Plan B API
- **修复**：改用 `addTrack(track, stream)` 逐轨道添加，兼容 Unified Plan SdpSemantics
- **文件**：`lib/pages/webrtc/services/webrtc_controller.dart`
- **注意**：需要完全重建（`flutter clean` + `flutter run`），热重载不生效

### WebRTC 双方无法建立通话修复（2026-05-13 下午）
- **根因**：`startCall()` 中在 `_peerUid` 为空时就调用 `_createAndSendOffer()`，offer 发送给空字符串被服务端丢弃；同时客户端未处理 `peer-ready` 事件
- **根因一**：`onConnected` 覆盖 `_myUid` 为服务端 UUID，与自定义 UID 不一致
- **根因二**：`onUserJoined` 处理器为空，新用户加入后 `_peerUid` 未更新
- **根因三**：客户端未实现 `peer-ready` 事件处理，双方都不知道何时可以开始信令协商
- **根因四**（第二轮修复）：竞态条件——`peer-ready` 在 `_getLocalMedia()` 期间到达，但此时 `_pc` 尚未创建，`_createAndSendOffer()` 检查 `if (_pc == null) return;` 静默跳过
- **根因五**（第三轮修复）：**服务端要求所有转发消息（offer/answer/ice-candidate/end-call）必须携带 `roomId` 字段**，但客户端发送时只带了 `{to, sdp}`，服务端验证 `if (!data?.roomId) return;` 直接丢弃
- **修复方案**：
  1. `signaling_client.dart`：
     - 新增 `_currentRoomId`，`joinRoom()` 时记录
     - `sendOffer/sendAnswer/sendEndCall` 自动加上 `roomId`
     - 新增 `peer-ready` 事件解析 + `onPeerReady` 流
  2. `webrtc_controller.dart`：
     - `onConnected` 不覆盖 `_myUid`
     - `onUserJoined` 设置 `_peerUid`
     - 新增 `_handlePeerReady()`：按 UID 字典序排序，较小者作为 Offer 发起方
     - `startCall()` 移除过早发 offer，改为等待 `peer-ready`
     - `_handleOfferReceived()` 中如果 `_handshakeStarted=true` 则自动应答
     - 新增 `_pendingOffer` 标志，PC 创建后补发 offer
  3. 新增 `_createAndSendAnswer()` 方法
- **状态**：需 `flutter clean` + `flutter run` 完全重建验证

### Mesh 多人通话改造（2026-05-13 晚上）
- **目标**：在保留 1v1 的基础上扩展为 Mesh 多人通话（3-4 人）
- **服务端**：移除 `room.size === 2` 限制，每次新人加入都向所有人广播 `peer-ready` 含完整 peer 列表
- **客户端 WebrtcController**：
  - 单 PC 模式 → Map<String, RTCPeerConnection> 多 PC 模式
  - 每个对端独立的 MediaStream + RTCVideoRenderer
  - UID 字典序较大者主动发 Offer，较小者自动应答
  - `onUserLeft` 事件处理清理指定 peer
  - `_cleanup()` 遍历清理所有 peer
- **UI VideoCallPage**：
  - 单画面 → 动态 GridView（1人全屏、2人上下分屏、3-4人2x2网格）
  - `remotePeerUids` 可观察列表驱动网格渲染
  - 顶部栏显示在线人数
- **文件**：
  - `cubeverse/src/modules/webrtc/webrtc.service.ts`
  - `lib/pages/webrtc/services/webrtc_controller.dart`（完全重写）
  - `lib/pages/webrtc/pages/video_call_page.dart`（完全重写）

