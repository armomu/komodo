# 2026-05-10

## BleDemoDashboardPage Tab UI 重构
- 将 `ble_demo_dashboard_page.dart` 的 `DefaultTabController + AppBar bottom: TabBar` 改为仿短视频风格的 title tab 切换 UI
- 使用 `StatefulWidget` + `PageController` + `PageView` 替代原 `TabBarView`
- AppBar title 区域使用 `Row(mainAxisSize: min)` + `GestureDetector` 实现点击切换，激活项加粗+下划线，非激活项变灰
- 下划线使用 `AnimatedContainer` 实现宽度动画（0↔20）
- 颜色适配主题：`colorScheme.onSurface`（激活）/ `colorScheme.onSurfaceVariant`（非激活）/ `colorScheme.primary`（下划线）
- tabs: ['发送数据', 'OTA 升级']
- `flutter analyze` 无报错

## 蓝牙发送数据 Tab 自定义十六进制键盘 (2026-05-10)
- 新建 `lib/pages/ble_demo/widgets/hex_keyboard.dart`：自定义十六进制键盘 Widget
  - `HexKeyboard.show(context, value, onConfirm)` 静态方法弹出 BottomSheet
  - 键盘布局：7行×5列，包含 0-9、A-F；快捷键 00、FF；CLR 清空；⌫ 退格（长按清空）
  - 自动每 2 字符插入空格，最多 64 字节
  - 黑色终端风格输入显示区（黄色字体）
  - 深浅主题自适应
- 改造 `send_data_tab.dart`：
  - TextField 替换为 `_HexInputField`（只读展示框，点击弹出键盘）
  - `AnimatedBuilder` 监听 controller 实时刷新显示
  - 黑色背景 + 黄色等宽字体展示输入内容
- `flutter analyze` 零问题

## HexKeyboard 改用 AppBottomSheet (2026-05-10)
- 将 `hex_keyboard.dart` 的 `showModalBottomSheet` 替换为项目公共组件 `AppBottomSheet.show`
- `HexKeyboard.show` 改为无需传 context（`Get.bottomSheet` 内部不需要 context）
- 键盘内容通过 `child: _HexKeyboardBody(...)` 传入，header 用 `AppBottomSheetHeader`
- `trailing` 用 `ValueListenableBuilder` 实时更新字节计数
- `send_data_tab.dart` 调用改为 `HexKeyboard.show(value:, onConfirm:)`（去掉 context 和 await）
- 按键颜色全部改为深色半透明风格，与 AppBottomSheet 深色卡片背景匹配
