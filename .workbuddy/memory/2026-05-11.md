
## MessageTab 下拉刷新 (10:55)
- 将 `CustomScrollView` + `SliverList` 改为 `NestedScrollView` + `RefreshIndicator` + `ListView`
- 使用 `RefreshIndicator.onRefresh` 调用 `_loadConversations()` 实现下拉刷新

## MusicTab 下拉刷新 (21:30)
- 在 `NestedScrollView.body` 的 `ListView` 外包 `RefreshIndicator`
- 添加 `_onRefresh()` 方法（800ms 延迟占位，后续接入真实数据时在此加载）
