# 2026-06-10

## request.dart 优化：错误路径统一返回 ApiResponse
- 修改 `request.dart`：所有错误路径（401、业务错误、DioException、catch）不再 `throw`，改为 `return ApiResponse<T>.error(code, message)`
- `ApiResponse` 新增 `factory ApiResponse.error(int code, String message)` 构造方法
- 调用方现在通过 `isSuccess` 判断请求成败，不再依赖 try-catch
