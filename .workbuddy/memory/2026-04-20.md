# 2026-04-20

## KOMODO- 首页改造为底部5Tab导航

- 将原 `home_page.dart` 从单页面重构为底部5Tab导航框架
- Tab 结构：首页 / 短视频 / [+号FAB] / 消息 / 我的
- 中间+号使用 `FloatingActionButton` + `BottomAppBar`（CircularNotchedRectangle）实现凹槽效果，点击触发事件（无页面切换）
- 新增4个子页面文件：`lib/pages/home/tabs/home_tab.dart`、`short_video_tab.dart`、`message_tab.dart`、`profile_tab.dart`
- 「我的」页面含：AppBar带设置/扫一扫按钮、用户头像+名称信息区域、8格常用功能Grid
- 使用 `IndexedStack` 保持各Tab状态
- 后续调整：去掉 FAB，+号改为内联在 bottomNavigationBar（自定义 Container Row）里的圆角矩形按钮
- 短视频 Tab 改造为抖音风格：PageView 竖向滑动 + video_player 播放网络视频，右侧点赞/评论/收藏/分享操作栏，顶部透明 AppBar（关注/精选/同城），使用两个公开视频链接循环复用（butterfly.mp4 / mov_bbb.mp4）
- 短视频顶部三个 Tab（关注/精选/同城）改为水平 PageView 滑动切换；关注和精选共享同一个 VideoFeedController 视频流；同城 Tab 为图文左右双列布局（Card + picsum 封面图 + 标题/距离），浅灰背景
- 新增直播 Tab：创建 `lib/pages/home/tabs/live_tab.dart`，使用 flutter_vlc_player 播放 rtmp://live.hkstv.hk.lxdns.com/live/hks
- 直播 Tab UI：顶部标题栏（头像+直播名+在线人数）+ VLC 视频画面 + 右侧操作按钮 + 下方 Tab（聊天/投票/赛况）+ 聊天输入框
- pubspec.yaml 添加依赖：`flutter_vlc_player: ^7.4.1`
