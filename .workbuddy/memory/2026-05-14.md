# 2026-05-14 日志

## 完成的工作

### 消息聊天简化版 1v1 视频通话
- **新增文件**：
  - `lib/pages/message/controllers/video_call_controller.dart`：精简版 GetxController，复用全局 SignalingClient，纯 1v1，先开摄像头再连信令
  - `lib/pages/message/pages/video_call_page.dart`：1v1 视频通话 UI（远端全屏 + 本地面中画 + 名字标签 + 底部控制栏）
  - 参数通过 Get.arguments 传入（serverUrl/roomId/myUid/peerName）
- **修改文件**：
  - `lib/pages/message/widgets/expanded_icon_bar.dart`：新增 `onVideoCallTap` 视频通话按钮
  - `lib/pages/message/chat_page.dart`：新增 `_startVideoCall()` 方法，接入 ExpandedIconBar
  - `lib/routes/app_routes.dart`：新增 `/chat-video-call` 路由
- **原有 webrtc 模块未改动**
- 入口：聊天页 → 展开工具栏 → "视频"按钮

### 登录/注册/用户信息改造
- **模型**：扩展 `LoginResult` 添加 id/email/nickname/avatar；新增 `UserProfile` 模型
- **API 服务**：`api_service.dart` 新增 `patch<T>()` 方法
- **UserController**：
  - `login()`：路径改为 `/consumer/auth/login`，参数改为 email+password，保存完整用户信息
  - `register()`：新增 `/consumer/auth/register` 接口，email+code+password 注册
  - `sendCaptcha()`：新增 `/auth/mail/captcha?email=xxx` 发送验证码
  - `updateProfile()`：新增 PATCH `/consumer/auth/profile`，修改 nickname/avatar
  - `logout()`：调用 POST `/consumer/auth/logout` 后清除本地状态
  - 用户信息持久化到 GetStorage（id、email、nickname、avatar）
- **登录页**：username 改为 email，增加邮箱格式验证，底部添加注册入口
- **注册页**：`register_page.dart` 新建（邮箱+验证码+密码+确认密码+60s倒计时）
- **编辑资料页**：`profile_edit_page.dart` 新建（昵称编辑+8个 picsum.photos 头像选择+保存按钮）
- **头像数据**：`utils/avatar_data.dart` 新建，8个在线头像URL
- **ProfileTab**：Obx 动态展示——非登录显示红色占位+ "登录" 文字，点击→登录页；登录后显示服务端 nickname + avatar，点击→编辑页
- **设置页**：底部添加红色"注销登录"按钮+确认对话框，调用 logout 后跳回首页
- **路由**：新增 `/register` 和 `/profile-edit` 两个路由
- **验证**：`flutter analyze` 零错误通过
